sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t utkarsh132/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true utkarsh132/docker-react npm run test
